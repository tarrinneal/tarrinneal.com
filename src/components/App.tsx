import * as React from 'react';

type AppProps = { num: number };

export const App = ({ num }: AppProps) => (
  <>
    <h1>This is Tarrin Neal's website! Please hire him!</h1>
    <p>Built with React hooks and Typescript</p>
  </>
);
